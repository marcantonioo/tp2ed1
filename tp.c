#include <stdio.h>
#include "domino.h"

int main(){
    int n;
    Dominol  *jogo;
    jogo = DominoCria(n);//cabeça
   
    do{
        scanf("%d", &n);
        for(int i = 0; i < n; i++){
            scanf()
        }


    }while(n>0);
}